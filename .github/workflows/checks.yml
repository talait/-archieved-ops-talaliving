# The checks this project has been running by hand for sixty-three milestones.
#
# Every one of them was real and every one of them was manual: typed into a
# terminal, read once, reported in a commit message, and gone. That is fine
# while one person is building and worthless the moment a second one is (F96).
#
# The order is deliberate — cheapest first, so a missing type is not discovered
# after a four-minute browser run.
name: checks

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  checks:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - run: npm ci

      - name: Lint
        run: npm run lint

      - name: Types
        run: npx tsc --noEmit

      # The ratchet. Today's gap is in KNOWN_GAP and passes; new drift fails.
      # Shrinking that list is the only honest progress bar for the swap (F94).
      - name: API seam conformance
        run: npm run check:api

      # Duplicate keys and dangling references in the seed. Two bugs of this
      # shape (F84, F86) were found by accident after six milestones each; this
      # one found four more (F98). It needs nothing running, so it is cheap.
      - name: Fixture integrity
        run: npm run check:fixtures

      - name: Build
        run: npm run build

      # The refusal table — the evidence line in thirty-odd milestone entries,
      # and until F96 the only one nobody but its author could reproduce.
      - name: Install Chromium
        run: npx playwright-core install --with-deps chromium

      # A curl loop rather than `npx wait-on`: one less package fetched at run
      # time, and one less thing that can be unavailable on the morning somebody
      # needs the result.
      - name: Start the app
        run: |
          npm run start -- -p 3100 &
          for i in $(seq 1 60); do
            if curl -fsS -o /dev/null http://localhost:3100/demo; then
              echo "up after ${i}s"; exit 0
            fi
            sleep 1
          done
          echo "the app did not answer on :3100 within 60s"; exit 1

      - name: Refusal probes
        run: npm run check:refusals
        env:
          PROBE_URL: http://localhost:3100

  # The route sweep opens every route, then every link in each of the eight
  # accounts' own menus — 179 account-and-route combinations, about ten minutes.
  # Too slow to sit in front of every push on a feature branch, and too useful to
  # leave out: it is the only check that looks at the app as somebody other than
  # whoever wrote the screen (F61, F97). So it runs where a slow answer is still
  # a useful one — pull requests, and main.
  sweep:
    if: github.event_name == 'pull_request' || github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npx playwright-core install --with-deps chromium
      - run: npm run build
      - name: Start the app
        run: |
          npm run start -- -p 3100 &
          for i in $(seq 1 60); do
            if curl -fsS -o /dev/null http://localhost:3100/dashboard; then
              echo "up after ${i}s"; exit 0
            fi
            sleep 1
          done
          echo "the app did not answer on :3100 within 60s"; exit 1
      - name: Route and menu sweep
        run: npm run check:routes
        env:
          PROBE_URL: http://localhost:3100

      # 390 / 768 / 1440 — the claim the milestone board made twice by eye, and
      # which was false on six routes when something finally measured it (F100).
      - name: Layout sweep
        run: npm run check:layout
        env:
          PROBE_URL: http://localhost:3100
